unbind r
bind r source-file ~/.tmux.conf

set -g prefix C-s
set -g mouse on
set-option -g set-clipboard on

bind-key h select-pane -L
bind-key j select-pane -D
bind-key k select-pane -U
bind-key l select-pane -R

# Plugins
set -g @plugin 'tmux-plugins/tpm'
run-shell "~/.tmux/plugins/custom-onedark-theme/tmux-onedark-theme.tmux"

set-option -g status-position top

# Load plugins
run '~/.tmux/plugins/tpm/tpm'

# set -g status-style "bg=#1f2329,fg=#a0a8b7";

# setw -g window-status-style "bg=#1f2329,fg=#7a818e"
# setw -g window-status-current-style "bg=#1f2329,fg=#4fa6ed"
# set -g pane-border-style "fg=#30363f"
# set -g pane-active-border-style "fg=#4fa6ed"
